# Fix Prisma Table Missing Error ✅

- [x] Run `cd server && npx prisma migrate deploy` to apply migrations
- [x] Optionally run `cd server && npx prisma db seed` for demo data
- [ ] Restart server (stop current server process if running, then `cd server && npm run dev` or similar)
- [ ] Test login and /me endpoints (e.g., POST /login with email: admin@dental.local, password: Demo12345!)
