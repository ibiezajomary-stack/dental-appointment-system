/**
 * Legacy export — the chart is now paper-style with per-surface charting.
 * @deprecated Use `PaperStyleOdontogram` with `patientId`.
 */
export { PaperStyleOdontogram as InteractiveOdontogram } from "./PaperStyleOdontogram";
